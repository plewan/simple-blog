<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Blog{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
        <header>
            <h1>
                Blog
            </h1>
            <!-- nav bar -->
            <nav>
                <a href="{{ path('homepage') }}">Strona Główna</a>
                <a href="{{ path('register') }}">Rejestracja</a>
            </nav>
        </header>

        <main>
            {% block body %}{% endblock %}
        </main>

        <footer>
            <p>All Rights Reserverd @plewan 2021</p>
        </footer>
    </body>
</html>
